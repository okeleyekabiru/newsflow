# Disable Docker Compose's bake backend — it has a Windows path bug in Buildx < 0.21
# where absolute Dockerfile paths get doubled (context + absolute = broken path).
# Remove this line once Docker Desktop ships a Buildx fix.
COMPOSE_BAKE=false
